<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifam.cromograme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EstudoEstruturasActivity"></activity>
        <activity android:name=".activity.JogoEstruturasActivity" />
        <activity android:name=".activity.JogoDoencasActivity" />
        <activity android:name=".activity.JogoCromossomoActivity" />
        <activity android:name=".activity.JogoDivisaoCelularActivity" />
        <activity android:name=".activity.EstudoDoencasActivity" />
        <activity android:name=".activity.EstudarActivity" />
        <activity android:name=".activity.JogarActivity" />
        <activity android:name=".activity.EstudoCromossomoActivity" />
        <activity android:name=".activity.EstudoDivCelActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>